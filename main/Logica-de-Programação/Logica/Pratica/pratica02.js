// operadores

var nome = "Marcos"; // variavel recebe nome
var idade = "idade"; // variavel recebe idade

var res = nome == idade ? "sim" : "Nao";
/* var res nome e igual a idade?
resposta: não

var res não e igual a idade
resposta: sim

*/
console.log(res);

var n1 = 8; // var recebe 8
var n2 = 2; // var recebe 2

var rest = n1 % n2 == 0 ? "Sim" : "Não";
/* var rest divido por 2 sobra 0
resposta: Sim

var rest divido por 2 não sobra 0
resposta: Não

*/
console.log(rest);
