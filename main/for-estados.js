// variavel com  a array de estados
let estados = ["Amazonas", "Minas Gerais", "São Paulo", "Acre"]
// ira encrecentar posições na função estado
for(let pos = 0; pos <  estados.length; pos++){
    console.log(estados[2])
}

// ira mostrar o estado da posição 02